buildscript {
    repositories {
        mavenLocal()
        maven {}
    }

    dependencies {
        classpath "org.roboscratch.gradle:gradle-robot:0.1.0"
    }
}

apply plugin: "org.roboscratch.robot"
